# Развёртывание удалённого API

Основной `docker-compose.yml` предназначен только для сервера. Он поднимает
удалённый API, запускает миграции Alembic и подключается к существующей
PostgreSQL. Локальный frontend и Chrome на сервере не запускаются.

PostgreSQL и pgAdmin в Compose намеренно отсутствуют: база уже существует и не
должна быть доступна пользовательским компьютерам.

## Первый запуск

Получите проект:

```bash
git clone https://github.com/ordenmeny/cdp_parsing.git
cd cdp_parsing
```

Создайте серверный файл настроек:

```bash
cp .env.remote.example .env.remote
```

Заполните в `.env.remote` адрес, порт, имя базы, пользователя, пароль и токен
API. Случайный токен можно создать командой:

```bash
openssl rand -hex 32
```

`.env.remote` хранится только на сервере и не попадает в Git. Такой же токен
передаётся разрешённым пользователям в их локальном `.env`, но реквизиты БД им
не передаются.

## Запуск одной командой

```bash
docker compose up -d --build
```

Контейнер сначала автоматически выполняет:

```text
alembic upgrade head
```

и только после успешной миграции запускает удалённый FastAPI на порту `8001`.

Проверка:

```bash
docker compose ps
docker compose logs -f remote-api
```

## Подключение пользователей

API публикуется напрямую на порту `8001` внутри локальной сети. В `.env`
пользователя укажите внутренний IP-адрес или локальное DNS-имя сервера:

```dotenv
PARSER_REMOTE_API_URL=http://<внутренний-IP-сервера>:8001
```

Если на сервере включён firewall, разрешите входящие TCP-подключения к порту
`8001` только из пользовательской подсети. Порт PostgreSQL открывать не нужно.

Контейнер использует сеть Linux-хоста. Это позволяет ему обращаться к
PostgreSQL через те же локальные маршруты или VPN, которые настроены на
сервере.

Маршруты находятся под `/api/v1` и требуют заголовок:

```text
Authorization: Bearer <PARSER_REMOTE_API_TOKEN>
```

## Обновление

```bash
git pull
docker compose up -d --build
```

## Остановка

```bash
docker compose down
```

Не добавляйте `--volumes`, если не требуется удалить временные файлы заданий.
Данные продавцов находятся во внешней PostgreSQL и командой `docker compose
down` не удаляются.

## Локальный Docker-контейнер пользователя

Он вынесен в отдельный файл и на сервере не запускается:

```bash
docker compose -f docker-compose.local.yml up -d --build
```

Обычному пользователю Docker не требуется: локальный запуск через `uv` и npm
описан в корневом `README.MD`.
